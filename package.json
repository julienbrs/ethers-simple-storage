{
  "dependencies": {
    "dotenv": "^16.0.3",
    "env": "^0.0.2",
    "ethers": "^5.7.2",
    "fs-extra": "^10.1.0",
    "prettier": "^2.7.1",
    "prettier-plugin-solidity": "^1.0.0-rc.1",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "solcjs --bin --abi --optimize --overwrite -o build/contracts *.sol"
  }
}
